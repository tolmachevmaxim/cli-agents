interface:
  display_name: "CLI Agents"
  short_description: "Delegate tasks to CLI agents and Aider"
  default_prompt: "Use $cli-agents to safely delegate this task to another CLI agent or Aider."
policy:
  allow_implicit_invocation: true
